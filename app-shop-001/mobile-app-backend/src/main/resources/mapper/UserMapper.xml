<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.backend.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.shop.backend.model.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="UserColumns">
        id, username, password, email, role, created_at, updated_at
    </sql>

    <select id="findById" resultMap="UserResultMap">
        SELECT <include refid="UserColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT <include refid="UserColumns"/>
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT <include refid="UserColumns"/>
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT <include refid="UserColumns"/>
        FROM users
        ORDER BY id
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="existsByUsername" resultType="int">
        SELECT COUNT(*) FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="com.shop.backend.model.User">
        <selectKey keyProperty="id" order="BEFORE" resultType="long">
            SELECT users_seq.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO users (id, username, password, email, role, created_at, updated_at)
        VALUES (#{id}, #{username}, #{password}, #{email}, #{role}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.shop.backend.model.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email = #{email},
            role = #{role},
            created_at = #{createdAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <delete id="deleteAll">
        DELETE FROM users
    </delete>

    <insert id="insertWithId" parameterType="com.shop.backend.model.User">
        INSERT INTO users (id, username, password, email, role, created_at, updated_at)
        VALUES (#{id}, #{username}, #{password}, #{email}, #{role}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectMaxId" resultType="long">
        SELECT NVL(MAX(id), 0) FROM users
    </select>
</mapper>
