<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.backend.mapper.OrderItemMapper">

    <resultMap id="OrderItemResultMap" type="com.shop.backend.model.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="quantity" column="quantity"/>
        <result property="lineTotal" column="line_total"/>
    </resultMap>

    <sql id="OrderItemColumns">
        id, order_id, product_id, product_name, unit_price, quantity, line_total
    </sql>

    <select id="findByOrderId" resultMap="OrderItemResultMap">
        SELECT <include refid="OrderItemColumns"/>
        FROM order_items
        WHERE order_id = #{orderId}
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.shop.backend.model.OrderItem">
        <selectKey keyProperty="id" order="BEFORE" resultType="long">
            SELECT order_items_seq.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO order_items (id, order_id, product_id, product_name, unit_price, quantity, line_total)
        VALUES (#{id}, #{orderId}, #{productId}, #{productName}, #{unitPrice}, #{quantity}, #{lineTotal})
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM order_items WHERE order_id = #{orderId}
    </delete>
</mapper>
